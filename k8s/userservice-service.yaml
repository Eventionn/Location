apiVersion: v1
kind: Service
metadata:
  name: locationservice
  labels:
    app: locationservice
spec:
  selector:
    app: locationservice
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 5005        #Porta Interna Serviço 
      targetPort: 5005  #Porta Pod
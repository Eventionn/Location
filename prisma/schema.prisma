

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
  binaryTargets = ["native", "linux-musl"]
}

model Location {
  localtown String  @id @default(uuid()) @db.Uuid
  city        String  @db.VarChar(20)

  @@map("Locations") 
}
